# ⛔ 发布冻结（自 2026-09-23 起生效）

**在下列安全项全部完成前，禁止向本仓库推送任何提交** —— 推送会触发 Render 自动部署。

---

## 冻结原因

| # | 问题                                           | 实测证据                                                                                                    |
| - | -------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 1 | 线上仍在用**已泄露的弱 JWT 密钥**                        | 用 `hongrui-boss-secret-key-2024` 签发的 `{id:4}` token 调 `/api/analysis/dashboard` → **HTTP 200**，返回真实业务数据 |
| 2 | Render 的 `PG_PASSWORD` 为**已泄露的旧密码**          | 该密码存在于本仓库公开历史提交 `ebdb1d1`                                                                               |
| 3 | Supabase `anon` / `service_role` key **未轮换** | 旧 key 曾硬编码于本地 5 个脚本                                                                                     |

问题 1 意味着：**任何人**都能伪造管理员 token 并取得后台完整权限。在它修好之前，  
任何代码发布都是在向一个可能已被他人掌控的系统追加功能。

---

## 解除条件

> ⚠️ **2026-09-24 核实修正**：本清单此前被整体勾选，但当日逐项实测发现 **条件 1 / 2 / 3 / 6 并未成立**。  
> 现已按实测结果重新标注。核实脚本：`_governance/_verify-freeze.cjs`（只读）。

- [ ] Render 的 `JWT_SECRET` 已更换为强随机值（见 `_governance/ROTATION-KEYS.txt`）
      —— **实测未完成**：用弱密钥 `hongrui-boss-secret-key-2024` 伪造 `{id:4}` 调线上 `/analysis/dashboard` → **HTTP 200**
- [ ] Render 的 `PG_PASSWORD` 已更换为新的数据库密码
      —— **实测未完成**：本机 `.env` 的密钥签 token 打线上仍 **200**，说明线上与本机同密钥，未轮换
- [ ] Supabase `anon` / `service_role` key 已轮换 —— **未核实**（无直接证据）
- [x] 本地 `hongrui-boss/.env` 已同步上述新值（前提是上面已轮换）
- [ ] 后台管理员 `13213857001` 弱密码已更换 —— **未核实**
- [x] `ztest_admin` / `ztest_manager` / `ztest_employee` 三个测试账号已停用
      —— 2026-09-24 执行：3 个账号 `status` 置 0（原本均启用，其中 `ztest_admin` 为 admin 角色）
- [x] 冷启动验证通过：登录成功 + 14 个业务接口全 200 + 匿名请求全 401

---

## 2026-09-24 状态：**经决策后部分解冻发布**

老板决定「先发布、密钥稍后轮换」，因此本次推送是在**条件 1/2/3 仍开放**的情况下进行的，  
风险已知并接受。推送后**必须尽快完成密钥轮换**，否则越权漏洞持续存在。

---

## 推送后待办（优先级从高到低）

1. 在 Render 控制台轮换 `JWT_SECRET`（强随机值，见 `_governance/ROTATION-KEYS.txt`）
2. 在 Render 控制台轮换 `PG_PASSWORD`（同时更新 Supabase 数据库密码）
3. 轮换 Supabase `anon` / `service_role` key，并同步本地 `hongrui-boss/.env`
4. 更换后台管理员 `13213857001` 的密码
5. 全部完成后，用 `_governance/_verify-freeze.cjs` 复验本清单
